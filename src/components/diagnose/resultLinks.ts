/** 마이페이지 "내 진단 결과" 가 붙이는 `?from=` 값 — 결과 화면의 뒤로가기가 매거진 대신 마이페이지로 간다. */
export const FROM_MY_PAGE_PARAM = 'my-page';

export const BODY_TYPE_RESULT_FROM_MY_PAGE_HREF = `/magazine/personal-body-type/diagnose/result?from=${FROM_MY_PAGE_PARAM}`;
