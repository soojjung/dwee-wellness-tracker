# dwee 운영 로그

TestFlight·출시 이후 **무슨 일이 있었고 무엇을 했는지** 시간순으로 남기는 기록. 항목별 통과 여부는 [testflight-checklist.md](./testflight-checklist.md) 가, 출시 계획은 [release-plan-v1.md](./release-plan-v1.md) 가 원본이고, 이 문서는 그 사이의 사건·원인·조치를 적는다.

작성 규칙

- 라운드(빌드) 단위 절. 새 빌드를 올리면 "빌드 이력" 표에 한 줄 + 새 라운드 절을 연다.
- 피드백 1건 = 표 1줄. 번호는 라운드마다 1부터 (`R3-1` 처럼 참조). 커밋 메시지에도 같은 번호를 쓴다.
- **원인**은 추측이면 "추정" 을 붙이고, 확인되면 고친다. 재현 못 한 건은 상태 `관찰` 로 남긴다.
- 코드 밖 조치(콘솔 설정, Supabase SQL, Sentry, App Store Connect)는 "운영 조치" 표에 적는다 — git 에 흔적이 안 남는 것들.

상태: `수정` 커밋됨 · `확인` 다음 빌드에서 실기기 확인 완료 · `관찰` 재현 대기 · `보류` 이번 릴리스에서 안 함 · `재발` 확인 후 다시 발생

## 빌드 이력

| 빌드 | 버전 | 업로드 | 담긴 PR | 비고 |
|---|---|---|---|---|
| (Xcode 직접) | 1.0.0 | 2026-09-24 | — | 실기기 QA 라운드 1 (iPhone 13). 수정은 #74 |
| 2 | 1.0.0 (2) | 2026-09-24 | #74, #75 | 첫 TestFlight 빌드. 외부 테스팅 심사 제출 |
| 3 | 1.0.0 (3) | 2026-09-24 이후 | #76 | 라운드 2 피드백 수정 |
| 4 | 1.0.0 (4) | — | (라운드 3 PR) | 라운드 3 피드백 수정 예정 |

## 라운드 3 — 빌드 3 피드백 (2026-09-26 ~)

브랜치 `fix/testflight-round-3` (main `1ea4a6e` 기준). 다 모이면 `iosBuild` 3→4 커밋 후 PR 한 번.

| # | 날짜 | 화면 | 증상 (보고 그대로) | 원인 | 조치 | 커밋 | 상태 |
|---|---|---|---|---|---|---|---|
| R3-1 | 09-26 | 로그인 → 이용약관·개인정보 처리방침 | 첫 진입 시 약관/방침을 누르면 뒤로가기 버튼이 없어 나갈 수 없음 | 공개 `/legal/*` 셸은 웹(앱스토어 URL) 용으로 워드마크만 두고 뒤로가기가 없음. 네이티브엔 브라우저 뒤로가기도 없음 | 앱 안에서 열었을 때(`hasInAppHistory`) 워드마크 자리에 마이페이지와 같은 뒤로가기 버튼(폴백 `/login`, 스와이프 백 포함). 언어 토글은 `replace` 로 기록을 안 쌓음. 뒤로가기 버튼 스크린리더 이름을 "닫기" → "뒤로/Back"(`myPage.backAriaLabel`, 앱 내 약관·방침·준비 중 화면 포함) | (이 커밋) | 수정 |
| R3-2 | 09-26 | 홈 꾸미기 | 사진 제거 후 뒤로가기 → 재진입 시 이전에 고른 장수(1장)가 선택된 채로 남음 (사진 없음·편집 완료 비활성) | 편집 화면에서 사진을 지워도 draft 의 `draftPhotoCount` 는 유지되고, 재진입 시 `beginPhotoDraft` 가 활성 draft 면 아무것도 안 함 | 재진입 시 선택한 장수의 칸이 다 채워져 있지 않으면(전부 삭제·일부 삭제) 장수 선택과 확인 상태를 초기화. 고른 사진은 draft 에 남고 뒤로가기 시 폐기 확인은 그대로 | (이 커밋) | 수정 |
| R3-3 | 09-26 | 매거진 아티클 (퍼스널 체형) | "내 체형 알아보기" 하단 버튼 아래 흰색 여백 노출 (iPhone18,3, iOS 26.6.2) | 고정 하단 바가 `pb-[safe-area-inset-bottom]` 영역을 흰 배경(`gray50`)으로 두고 버튼 색은 안쪽 링크에만 칠함 → 홈 인디케이터 영역이 흰 띠 | 버튼 색을 바깥 바에 칠해 safe-area 까지 버튼으로 채움. 같은 구조인 체형 진단 인트로 "사진 선택" 바(pink50)도 함께 수정 | (이 커밋) | 수정 |
| R3-4 | 09-26 | 다이어리 꾸미기 · 스티커 보관함 | 바텀시트를 끝까지 내려도 스티커가 보임. "스티커 보관함" 제목만 보이게 | peek 스냅이 화면 높이 85% 고정(`85dvh`) → 402×874 기기에서 시트가 ~131px 노출, 헤더(86px) 아래 스티커 그리드 ~45px 가 보임 | `DraggableBottomSheet` 에 `peekVisiblePx` 추가: peek = 헤더 86px(핸들 14 + 제목 행 72) + 홈 인디케이터 inset. 화면 높이와 무관하게 제목까지만 노출 | (이 커밋) | 수정 |
| R3-5 | 09-26 | 다이어리 꾸미기 · 스티커 보관함 `+` | 스티커 만들 때 별도 앨범 선택/사진 찍기 메뉴가 뜸 → 다른 화면처럼 iOS 기본 메뉴로 | `StickerAddMenu` 가 자체 다크 팝오버를 그림 (홈 꾸미기 등은 iOS 시스템 UI) | 앱에서는 `@capacitor/action-sheet` 로 iOS 액션 시트(앨범 선택 / 사진 찍기 / 취소), 웹은 기존 팝오버. "사진 찍기"는 누끼(스티커 모드)를 위해 인앱 카메라 유지 — 사용자 결정. 새 사전 키 `addStickerCancel`, en `chooseAlbum` "Choose from album" → "Photo Library"(iOS 관례) | (이 커밋) | 수정 |
| R3-6 | 09-26 | 주기 리포트 · 생리 아이콘(+) | 생리 날짜 수정은 홈의 생리 아이콘에서만 가능. 리포트의 같은 아이콘도 홈과 같은 팝업이면 좋겠음 (iPhone14,5, ko-US) | 리포트 아이콘·빈 상태 버튼은 새 기록 추가 전용 `LogEntryDialog`(시작/종료 + 컨디션·메모)를 열어 기존 기록 수정·삭제가 불가 | 리포트도 홈과 같은 `PeriodSelectSheet` 를 연다. 저장 로직(삭제→수정→추가 + 온보딩 완료 표시)을 `useApplyPeriodChanges` 훅으로 모아 두 화면이 공유. 안 쓰이게 된 `LogEntryDialog` 삭제 (컨디션 입력 제거는 사용자 확인, 사전 키 6개는 미정리). 후속: 과거 입력 범위 6개월 → 12개월 + "이전 달 더 보기"(12개월씩, 스크롤 위치 보정) + 더 오래된 기록이 있으면 그 달까지(`monthsBackToCover`). 올해 아닌 달 헤더에 연도 표시 | (이 커밋) | 수정 |
| R3-7 | 09-26 | 다이어리 꾸미기 · 사진 선택 방식 시트 (누끼로 만들기 / 사진 그대로 사용) | 시트가 화면 하단에 밀착돼 여백이 부족해 보임 → 하단 약 142px 여백 요청 (iPhone18,3) | `PhotoImportModal` 본문 하단 여백이 `pb-6`(24px)뿐, safe-area 도 미반영 | 본문 하단 여백 142px (홈 인디케이터보다 커서 safe-area 별도 가산 없음). 402×874 기준 90dvh 안에 스크롤 없이 들어감 | (이 커밋) | 수정 |
| R3-8 | 09-26 | 홈 · 이런 활동 어때요? 카드 | "감정 기록" 제목이 옆 "명상과 호흡"보다 아래에 있음 → 위쪽으로 통일 (iPhone14,5) | 카드가 `justify-between` 이라 뱃지는 위, 제목+설명 묶음은 아래로 붙음 → 설명이 짧은 카드는 제목이 내려감 | 위 정렬로 변경: 모든 카드에서 제목이 뱃지 아래 같은 높이(카드 상단에서 56px)에 오고 남는 공간은 아래로. 4개 단계 카드 전부 측정 확인 | (이 커밋) | 수정 |

### 운영 조치

| 날짜 | 대상 | 내용 |
|---|---|---|
| 09-26 | iOS 네이티브 플러그인 | `@capacitor/action-sheet` 6.0.4 추가 (R3-5). 빌드 4 전 `pnpm cap:ios` 로 cap sync·pod install 필요 |

## 라운드 2 — 빌드 2 피드백 (2026-09-24) · PR #76

| # | 화면 | 증상 | 원인 | 조치 | 상태 |
|---|---|---|---|---|---|
| R2-1 | 홈 꾸미기 | 로그인 상태에서 사진 4장 "편집 완료" 눌러도 반응 없어 보임 | 원본(3~5MB) 순차 업로드, 진행 표시 없음, 실패는 store error 에만 | "저장 중" 표시·중복 탭 차단·실패 토스트(초안 유지), 2048px 다운스케일 | 수정 |
| R2-2 | 홈·다이어리 | 이미지 많은 화면 로딩 느림, 홈 기본 히어로가 잠깐 보였다 바뀜 | 매 실행마다 Storage 재다운로드 + hydrate 쿼리 ~19개, hydrate 전 기본 이미지 렌더 | IndexedDB blobCache, `getHomeDecor()` 2쿼리 배치, hydrate 전 회색 박스 | 수정 |
| R2-3 | 로그인 | "로그인 없이 시작" 이 1회 "익명 세션을 시작할 수 없어요" (03:57 KST, Wi-Fi), 몇 분 뒤 정상 | 미확인 — 에러가 화면에서 삼켜져 Supabase 응답 불명 | 익명·OAuth 시작 실패를 Sentry 로 (`auth_step/status/code` 태그) | 관찰 |
| R2-4 | 홈 | 긴 활동 카드 제목에서 이모지가 카드 밖으로 | 제목 줄바꿈 없음 | 줄바꿈 허용 | 수정 |
| R2-5 | 로그인 | 로그인 중 히어로 크기 변동 | 동의 행이 언마운트되며 레이아웃 변화 | 로그인 중에도 동의 행 유지 | 수정 |
| R2-6 | 스플래시 | 첫 실행 스플래시 없이 온보딩 1로 바로 | 2초 홀드를 navigation start 기준으로 계산 → WebView 부팅에 소진 | 스플래시 표시 시점부터 계산 | 수정 |

운영 메모: Sentry "App Hang 55초" (다이어리, 백그라운드 복귀 후) — 조사하지 않기로 함.

## 라운드 1 — 실기기 QA (2026-09-24) · PR #74

safe-area ~30화면, 스와이프 뒤로가기, 스티커 핀치(회전 seam 버그), 알림 설정 동기화(마이그레이션 0016 — 프로덕션 SQL 에디터로 적용), 네이티브 공유. 남은 버그 없음.
