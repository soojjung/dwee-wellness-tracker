# deps
node_modules

# next
.next
out

# typescript
*.tsbuildinfo

# capacitor native
ios
android

# env
.env*.local

# system
.DS_Store
*.log
.idea
.vscode

# claude code personal settings (commit settings.json, rules, CLAUDE.md)
.claude/settings.local.json

# playwright (per-run output; baselines under tests/snapshots/ are committed)
test-results/
playwright-report/
playwright/.cache/
playwright/.auth/

# supabase cli runtime cache (project ref, pooler url, version caches)
supabase/.temp/
supabase/.branches/
